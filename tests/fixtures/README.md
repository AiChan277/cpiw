# Test Fixtures

This directory contains resources for tests:
- `test_face.jpg`: A sample image containing a face for detection tests.
- `sample_config.yaml`: A mock configuration file.
- `expected_outputs/`: Directory containing expected array outputs for comparison.
